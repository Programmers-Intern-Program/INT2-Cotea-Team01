# 1829 (카카오프렌즈 컬러링북) 기대 응답 가이드

> B 튜닝용 체크리스트 | policy: `prompt-policy.json` v1.1.0  
> 검증: `dryRun: true`로 프롬프트 확인 → 실호출로 응답 비교

---

## Lv1 — BEFORE_SOLVE (관점 힌트)

**요청:** `hint_request_1829_l1.json`

### 포함해야 함
- 그리드/격자, **연결된 영역**, 상하좌우, 0은 탐색 제외
- 스스로 생각해볼 질문

### 금지
- DFS, BFS, Union-Find, 큐, 스택, visited
- 구현 순서, 코드, 의사코드

### 실측 샘플 (2026-07-09)
> "같은 숫자로 연결된 덩어리" … "상하좌우로 맞닿은 같은 값의 칸들을 전부 묶어내는 것" … ✅ 알고리즘명 없음

---

## Lv2 — BEFORE_SOLVE (접근 힌트)

**요청:** `hint_request_1829_l2.json`

### 포함해야 함
- BFS 또는 DFS (또는 그리드 탐색)
- 연결 영역 / 컴포넌트 개념
- 대안: Union-Find 언급 가능 (구현 X)

### 금지
- Java 코드, 완성 함수, 단계별 구현 순서

---

## Lv3 — SOLVING (구현 힌트)

**요청:** `hint_request_1829_l3.json`  
**질문:** "방문 처리를 어떻게 해야 할지 모르겠어요"

### 포함해야 함
- visited/boolean 배열 또는 방문 표시 개념
- 0 제외, 4방향, 영역 크기 누적
- 3~5단계 구현 순서 또는 의사코드 (핵심 로직 비움)

### 금지
- `dfs()` / `solution()` 전체 구현
- 복사해서 제출 가능한 코드

---

## Lv4 — SOLVING (코드 리뷰)

**요청:** `hint_request_1829_l4.json`

### 포함해야 함
- 제출 코드의 특정 라인/로직 지적
- 수정 방향 (경계 체크, 영역 카운트 등)

### 금지
- solution 메서드 전체 재작성
- 핵심 탐색 로직을 전부 채운 코드

---

## WRONG_ANSWER — 이유 질문 **전**

**요청:** `hint_request_1829_wrong_no_reason.json`  
**buttonId:** `wrong_result_only`

### 포함해야 함
- "오답이에요" / 결과 안내 1문장
- 스스로 점검하도록 되묻기

### 금지
- 0 칸 포함, 대각선 탐색 등 **구체적 원인** 나열
- commonMistakes 내용 그대로 전달

### ⚠️ 알려진 이슈
~~`ProblemContextSelector`가 이유 질문 전에도 진단 메타를 userMessage에 넣음~~ → v1.1.0에서 수정됨 (`beforeUserAskReason` 시 `fields` 비움).

---

## WRONG_ANSWER — 이유 질문 **후**

**요청:** `hint_request_1829_wrong.json`  
**질문:** "왜 틀렸는지 알려주세요"

### 포함해야 함
- 가능한 원인 1~2가지 (0 처리, 4방향, 방문 중복 등)
- 확인해볼 방향 힌트

### 금지
- 정답 코드
- 전체 솔루션

---

## TIME_LIMIT_EXCEEDED — 이유 질문 후

**요청:** `hint_request_1829_tle.json`

### 포함해야 함
- O(m*n) 대비 비효율 가능성 (중복 탐색, 불필요한 조합 등)
- 개선 방향 (visited, 한 번만 탐색)

### 금지
- 최적화된 전체 코드

---

## dryRun 검증 커맨드

```bash
cd backend
# 프롬프트만 (API Key 불필요)
curl -s http://localhost:8080/api/hint \
  -H "Content-Type: application/json" \
  -d "$(jq '. + {"dryRun":true}' examples/hint_request_1829_l1.json)"
```

확인 포인트:
1. `systemPrompt` — Lv forbid 규칙 포함 여부
2. `userMessage` — `fields`에 해당 Lv 필드만 있는지
3. WRONG_ANSWER — `오답 진단 정책` 문구가 before/after에 맞는지
